#include "PrintInfo.hpp"
#include <iostream>
#include <cmath>
using std::cin;
using std::cout;
using std::endl;
double GetPossibleGain(double bet, double winPercentage) {
    // return (100 / winPercentage) * bet
}
int main() {
    double balance = 1.5263;
    double bet     = 1000;
    double winPercentage;
    balance = round(balance * 100) / 100;
    cout << balance << endl;
    // PrintInfo(balance, bet, winPercentage, GetPossibleGain(bet, wwinPercentagee))
}
